import React, { Component } from 'react';
import styled from 'styled-components';

export default class DateFilter extends Component {
  handleClick(state) {}
  render() {
    return (
      <div>Boolean filter interface, but will need context input for empty or just NOT operations.</div>
    );
  }
}
