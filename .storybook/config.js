// import storybookReact from '@storybook/react';
// import storybookSvelte from '@storybook/svelte';

import './config.react';
// import './config.svelte';

// // automatically import all files ending in *.stories.js
// const reqReact = require.context('../stories', true, /\.stories\.js$/);
// function loadReactStories() {
//   reqReact.keys().forEach(filename => reqReact(filename));
// }

// const reqSvelte = require.context('../stories', true, /\.svelte-stories\.js$/);
// function loadSvelteStories() {
//   reqSvelte.keys().forEach(filename => reqSvelte(filename));
// }

// console.log(storybookReact)

// storybookReact.configure(loadReactStories, module);
// storybookSvelte.configure(loadSvelteStories, module);
